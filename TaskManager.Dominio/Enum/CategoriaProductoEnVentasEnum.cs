namespace TaskManager.Dominio.Enum
{
    public enum CategoriaProductoEnVentasEnum
    {
        Antipasto = 0,        // Entrantes
        PrimiPiatti = 1,      // Primeros platos (pastas, risottos)
        SecondiPiatti = 2,    // Segundos platos (carnes, pescados)
        Contorni = 3,         // Guarniciones
        Ensaladas = 4,        // Ensaladas
        Pizzas = 5,           // Pizzas
        Panes = 6,            // Panes y focaccias
        Postres = 7,          // Postres
        Bebidas = 8,          // Bebidas (no alcohólicas)
        Vinos = 9,            // Vinos
        Cervezas = 10,         // Cervezas
        Licores = 11,          // Licores y digestivos
        Cafes = 12,            // Café y tés
        Especialidades = 13   // Especialidades de la casa
    }
}
